---
const {label, tech1, tech2, tech3, customStyle} = Astro.props;
---
<div
  class=`bg-white border-2 rounded-4xl customShadow-sm w-[95vw] md:w-[75vw] lg:w-[30vw] px-4 py-4 hover:scale-[101%] transition duration-300 ease-in-out ${customStyle}`
>
  <h2 class="font-extrabold text-2xl">{label}</h2>
  <slot />
  <div class="flex flex-row items-center justify-around">
    <ul class="list-disc">
      <li>{tech1}</li>
      <li>{tech2}</li>
      <li>{tech3}</li>
    </ul>

    <div class="border-2 w-40 h-20 rounded-2xl bg-yellow-300"></div>
  </div>
</div>
