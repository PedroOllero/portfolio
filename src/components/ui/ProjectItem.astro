---
const {label, tech1, tech2, tech3, customStyle, role} = Astro.props;
---
<div
  class=`realtive z-10 bg-white border-2 rounded-4xl customShadow-sm w-[95vw] md:w-[75vw] lg:w-[30vw] px-8 py-8 hover:scale-[101%] transition duration-300 ease-in-out ${customStyle}`
>
  <h2 class="font-extrabold text-2xl">{role}</h2>
  <h3 class="font-light text-2xl mb-4">{label}</h2>
  <slot />
  <div class="flex flex-row items-center justify-around mt-4">
    <ul class="text-xl font-semiboldº">
      <li>{tech1}</li>
      <li>{tech2}</li>
      <li>{tech3}</li>
    </ul>

    <div class="border-2 w-40 h-20 rounded-2xl bg-yellow-300"></div>
  </div>
</div>
